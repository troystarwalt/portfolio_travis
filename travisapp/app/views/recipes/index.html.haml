%h1 Recipes#index
%p Find me in app/views/recipes/index.html.erb
- @recipes.each do |rec|
  %ul
    %li= rec.title
    %li= rec.content
    - if rec.admin_id != nil
      %li= rec.admin_username
    = link_to "(Delete)",  recipe_path(rec.id), method: :delete
  %hr